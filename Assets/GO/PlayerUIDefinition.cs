using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Vi.UI;

public class PlayerUIDefinition : PlatformUIDefinition
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
